const assert=require('assert'),fs=require('fs'),path=require('path'),vm=require('vm');const src=fs.readFileSync(path.join(__dirname,'..','appscript','src','Services','QuotePreparationService.gs'),'utf8');const c=vm.createContext({Number,String,Math,Object,Array,VmosValidationError:function(m){this.message=m},VmosNotFoundError:function(m){this.message=m}});vm.runInContext(src,c);const stored=[];const s=new c.QuotePreparationService({rfqs:{get:()=>({id:'RFQ-26-0128'})},quotes:{list:()=>stored,create:x=>{const q={id:'VQT-26-0128',...x};stored.push(q);return q;}}});const d=s.prepare({rfqId:'RFQ-26-0128',quantity:2,unitPrice:100,nre:50,paymentTerms:'Net 30',validity:'30 days',deliveryCommitment:'within 3 weeks of acceptance unless otherwise agreed upon'});assert.equal(d.total,250);assert.equal(d.lines[0].nre,50);const issued=s.issue(d);assert.equal(issued.status,'ISSUED');assert.equal(s.issue(issued).quoteId,issued.quoteId);const revised=s.prepare({rfqId:'RFQ-26-0128',quantity:2,unitPrice:110});assert.equal(revised.revision,1);console.log('VMOS quote preparation tests passed');
