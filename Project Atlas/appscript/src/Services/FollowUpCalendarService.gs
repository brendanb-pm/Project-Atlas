/** Generic MOS follow-up lifecycle; calendar providers are optional adapters. */
var FOLLOW_UP_STATUSES=['OPEN','COMPLETED','CANCELLED'];
function FollowUpService_(deps){this.deps=deps||{};this.repository=this.deps.repository;this.events=this.deps.events;this.lock=this.deps.lock||null;this.clock=this.deps.clock||function(){return new Date();};this.id=this.deps.id||function(prefix){return prefix+'-'+Utilities.getUuid().toUpperCase();};}
function createFollowUpService_(alreadyLocked){return new FollowUpService_({repository:new FollowUpRepository_(),events:new FollowUpEventRepository_(),lock:alreadyLocked?null:(typeof LockService!=='undefined'?LockService.getScriptLock():null)});}
FollowUpService_.prototype.create=function(input,actor){if(!input||!input.customerId||!input.title||!input.dueAt||!input.ownerUserId)throw new VmosValidationError_('Customer, title, due date, and owner are required.');this.assertSchedule_(input);var now=this.clock(),record={id:this.id('FUP'),customerId:input.customerId,salesActivityId:input.salesActivityId||'',title:input.title,dueAt:new Date(input.dueAt),startAt:input.startAt?new Date(input.startAt):'',endAt:input.endAt?new Date(input.endAt):'',timeZone:input.timeZone||'',ownerUserId:input.ownerUserId,status:'OPEN',version:1,createdAt:now,updatedAt:now,completedAt:'',cancelledAt:''};this.repository.create(record);this.event_(record,'CREATED',actor,'',0,1,this.schedulingClassification_(record));return record;};
FollowUpService_.prototype.reschedule=function(id,dueAt,expectedVersion,actor,correlationId){return this.withMutationLock_(function(){var record=this.repository.get(id);this.assertOpen_(record);if(Number(record.version)!==Number(expectedVersion))throw new VmosValidationError_('Follow-up changed elsewhere; refresh before rescheduling.');var previous=record.version;record.dueAt=new Date(dueAt);record.version=previous+1;record.updatedAt=this.clock();this.repository.update(id,record);this.event_(record,'RESCHEDULED',actor,correlationId,previous,record.version,'');return record;}.bind(this));};
FollowUpService_.prototype.schedule=function(id,schedule,expectedVersion,actor,correlationId){return this.withMutationLock_(function(){var record=this.repository.get(id);this.assertOpen_(record);if(Number(record.version)!==Number(expectedVersion))throw new VmosValidationError_('Follow-up changed elsewhere; refresh before scheduling.');this.assertSchedule_(schedule||{});if(!schedule.startAt)throw new VmosValidationError_('Start At, End At, and Time Zone are required for a scheduled calendar block.');var previous=record.version;record.startAt=new Date(schedule.startAt);record.endAt=new Date(schedule.endAt);record.timeZone=schedule.timeZone;record.version=previous+1;record.updatedAt=this.clock();this.repository.update(id,record);this.event_(record,'SCHEDULED',actor,correlationId,previous,record.version,this.schedulingClassification_(record));return record;}.bind(this));};
FollowUpService_.prototype.reassign=function(id,ownerUserId,expectedVersion,actor,correlationId){return this.withMutationLock_(function(){var record=this.repository.get(id);this.assertOpen_(record);if(!ownerUserId)throw new VmosValidationError_('A new Follow-up owner is required.');if(Number(record.version)!==Number(expectedVersion))throw new VmosValidationError_('Follow-up changed elsewhere; refresh before reassigning.');var previous=record.version,oldOwner=record.ownerUserId;record.ownerUserId=ownerUserId;record.version=previous+1;record.updatedAt=this.clock();this.repository.update(id,record);this.event_(record,'REASSIGNED',actor,correlationId,previous,record.version,JSON.stringify({previousOwner:oldOwner,newOwner:ownerUserId}));return record;}.bind(this));};
FollowUpService_.prototype.applyExternalSchedule=function(id,schedule,expectedVersion,provider,externalEventId,actor,correlationId){var record=this.repository.get(id);this.assertOpen_(record);if(Number(record.version)!==Number(expectedVersion))throw new VmosValidationError_('Follow-up changed elsewhere; refresh before applying calendar schedule.');this.assertSchedule_(schedule||{});if(!schedule.startAt)throw new VmosValidationError_('Start At, End At, and Time Zone are required for a scheduled calendar block.');var previous=record.version,prior={startAt:record.startAt||'',endAt:record.endAt||'',timeZone:record.timeZone||''};record.startAt=new Date(schedule.startAt);record.endAt=new Date(schedule.endAt);record.timeZone=schedule.timeZone;record.version=previous+1;record.updatedAt=this.clock();this.repository.update(id,record);this.event_(record,'FOLLOW_UP_RESCHEDULED_EXTERNALLY',actor||'CALENDAR:'+provider,correlationId,previous,record.version,JSON.stringify({provider:provider,externalEventId:externalEventId,previous:prior,next:{startAt:record.startAt,endAt:record.endAt,timeZone:record.timeZone}}));return record;};
FollowUpService_.prototype.complete=function(id,expectedVersion,actor,correlationId){return this.close_(id,'COMPLETED',expectedVersion,actor,correlationId);};
FollowUpService_.prototype.cancel=function(id,expectedVersion,actor,correlationId){return this.close_(id,'CANCELLED',expectedVersion,actor,correlationId);};
FollowUpService_.prototype.close_=function(id,status,expectedVersion,actor,correlationId){return this.withMutationLock_(function(){var record=this.repository.get(id);this.assertOpen_(record);if(Number(record.version)!==Number(expectedVersion))throw new VmosValidationError_('Follow-up changed elsewhere; refresh before closing.');var previous=record.version;record.status=status;record.version=previous+1;record.updatedAt=this.clock();if(status==='COMPLETED')record.completedAt=record.updatedAt;else record.cancelledAt=record.updatedAt;this.repository.update(id,record);this.event_(record,status,actor,correlationId,previous,record.version,'');return record;}.bind(this));};
FollowUpService_.prototype.assertOpen_=function(record){if(!record)throw new VmosNotFoundError_('Follow-up not found.');if(record.status!=='OPEN')throw new VmosValidationError_('Only open follow-ups can be changed.');};
FollowUpService_.prototype.assertSchedule_=function(input){var hasStart=!!input.startAt,hasEnd=!!input.endAt,hasZone=!!input.timeZone;if(!hasStart&&!hasEnd&&!hasZone)return; if(!hasStart||!hasEnd||!hasZone)throw new VmosValidationError_('Start At, End At, and Time Zone must be supplied together.');var start=new Date(input.startAt),end=new Date(input.endAt);if(isNaN(start.getTime())||isNaN(end.getTime())||end<=start)throw new VmosValidationError_('Scheduled calendar blocks require a valid end after start.');if(!/^[A-Za-z][A-Za-z0-9_+.-]*(?:\/[A-Za-z0-9_+.-]+)+$/.test(String(input.timeZone)))throw new VmosValidationError_('Time Zone must be an IANA identifier such as America/Los_Angeles.');};
FollowUpService_.prototype.schedulingClassification_=function(record){return record&&record.startAt&&record.endAt&&record.timeZone?'SCHEDULED':'LEGACY_DEADLINE_ONLY';};
FollowUpService_.prototype.event_=function(record,type,actor,correlationId,previousVersion,newVersion,details){this.events.append({id:this.id('FUE'),followUpId:record.id,eventType:type,occurredAt:this.clock(),actor:actor||'MOS',correlationId:correlationId||'',previousVersion:previousVersion,newVersion:newVersion,details:details||''});};
FollowUpService_.prototype.withMutationLock_=function(operation){if(!this.lock)return operation();this.lock.waitLock(10000);try{return operation();}finally{this.lock.releaseLock();}};

/** Provider capabilities make iCal publication visibly read-only. */
function GoogleCalendarFollowUpAdapter_(gateway){this.gateway=gateway;}
GoogleCalendarFollowUpAdapter_.prototype.capabilities=function(){return {provider:'GOOGLE_CALENDAR',providerKey:'GOOGLE_CALENDAR',twoWaySync:true,canCreate:true,canUpdate:true,canObserveChanges:true,canObserveDeletion:true,canVersion:true,supportsTwoWayReschedule:true,supportsPushNotifications:true,supportsDeltaSync:false,supportsPolling:true,supportsVersioning:true,supportsReadOnlyPublication:false,readOnlyPublication:false};};
GoogleCalendarFollowUpAdapter_.prototype.upsert=function(payload){return this.gateway.upsert(payload);};
function IcalFollowUpPublicationAdapter_(gateway){this.gateway=gateway;}
IcalFollowUpPublicationAdapter_.prototype.capabilities=function(){return {provider:'ICAL_PUBLICATION',providerKey:'ICAL_PUBLICATION',twoWaySync:false,canCreate:false,canUpdate:false,canObserveChanges:false,canObserveDeletion:false,canVersion:false,supportsTwoWayReschedule:false,supportsPushNotifications:false,supportsDeltaSync:false,supportsPolling:false,supportsVersioning:false,supportsReadOnlyPublication:true,readOnlyPublication:true};};
IcalFollowUpPublicationAdapter_.prototype.publish=function(payload){return this.gateway.publish(payload);};

function CalendarFollowUpSyncService_(deps){this.deps=deps||{};this.followUps=this.deps.followUps;this.links=this.deps.links;this.requests=this.deps.requests;this.syncEvents=this.deps.syncEvents;this.provider=this.deps.provider;this.clock=this.deps.clock||function(){return new Date();};this.timer=this.deps.timer||function(){return new Date().getTime();};this.lock=this.deps.lock||(typeof LockService!=='undefined'?LockService.getScriptLock():null);this.id=this.deps.id||function(prefix){return prefix+'-'+Utilities.getUuid().toUpperCase();};if(!this.syncEvents)throw new VmosConfigurationError_('Durable CalendarSyncEvents persistence is required for calendar synchronization.');}
CalendarFollowUpSyncService_.prototype.assertTwoWay_=function(){var caps=this.provider.capabilities();if(!caps.twoWaySync)throw new VmosConfigurationError_(caps.provider+' is read-only publication, not two-way synchronization.');return caps;};
CalendarFollowUpSyncService_.prototype.requestFields_=function(change){return {requestedDueAt:change.dueAt||'',requestedStartAt:change.startAt||'',requestedEndAt:change.endAt||'',requestedTimeZone:change.timeZone||''};};
CalendarFollowUpSyncService_.prototype.syncFromExternal=function(change){var acquired=false,start=this.timer();if(this.lock&&typeof this.lock.waitLock==='function'){this.lock.waitLock(30000);acquired=true;}try{change=change||{};change.totalStartedAt=start;if(change.providerCorrelationId){var providerLink=this.links.findByExternalEvent(change.provider,change.externalEventId);if(providerLink&&providerLink.lastCorrelationId===change.providerCorrelationId){change.followUpId=providerLink.followUpId;change.connectionId=providerLink.connectionId||'';change.operation='INBOUND_RECONCILE';return this.record_(change,'IGNORED','Loop prevented.');}}return this.syncFromExternalLocked_(change);}finally{if(acquired&&typeof this.lock.releaseLock==='function')this.lock.releaseLock();}};
CalendarFollowUpSyncService_.prototype.syncFromExternalLocked_=function(change){this.assertTwoWay_();if(!change||!change.correlationId||!change.externalEventId)throw new VmosValidationError_('Calendar change requires external event and correlation IDs.');var repositoryStarted=this.timer(),prior=this.syncEvents.findByCorrelation(change.provider,change.correlationId);change.repositoryDurationMs=(change.repositoryDurationMs||0)+(this.timer()-repositoryStarted);if(prior)return prior;var link=this.links.findByExternalEvent(change.provider,change.externalEventId);if(!link)return this.record_(change,'IGNORED','No linked MOS follow-up.');change.followUpId=link.followUpId;change.connectionId=link.connectionId||'';change.operation='INBOUND_RECONCILE';if(link.lastCorrelationId===change.correlationId)return this.record_(change,'IGNORED','Loop prevented.');if(change.changeType==='DELETED'){var deletion=this.requestFields_(change),request=this.requests.create({id:this.id('ECR'),provider:change.provider,followUpId:link.followUpId,externalEventId:change.externalEventId,changeType:'DELETED',requestedDueAt:deletion.requestedDueAt,requestedStartAt:deletion.requestedStartAt,requestedEndAt:deletion.requestedEndAt,requestedTimeZone:deletion.requestedTimeZone,externalVersion:change.externalVersion||'',status:'PENDING_REVIEW',details:'Calendar event was deleted externally.',detectedAt:this.clock(),resolvedAt:'',resolvedBy:'',resolution:''});return this.record_(change,'REVIEW_REQUIRED',request.id);}
var follow=this.followUps.repository.get(link.followUpId);if(change.mosVersion!==undefined&&Number(change.mosVersion)!==Number(follow.version)){var fields=this.requestFields_(change),conflict=this.requests.create({id:this.id('ECR'),provider:change.provider,followUpId:link.followUpId,externalEventId:change.externalEventId,changeType:'CONFLICT',requestedDueAt:fields.requestedDueAt,requestedStartAt:fields.requestedStartAt,requestedEndAt:fields.requestedEndAt,requestedTimeZone:fields.requestedTimeZone,externalVersion:change.externalVersion||'',status:'PENDING_REVIEW',details:'MOS version differs from calendar change.',detectedAt:this.clock(),resolvedAt:'',resolvedBy:'',resolution:''});return this.record_(change,'CONFLICT',conflict.id);}
var updated=change.startAt||change.endAt||change.timeZone?this.followUps.applyExternalSchedule(follow.id,{startAt:change.startAt,endAt:change.endAt,timeZone:change.timeZone},follow.version,change.provider,change.externalEventId,change.actor,change.correlationId):this.followUps.reschedule(follow.id,change.dueAt,follow.version,'CALENDAR:'+change.provider,change.correlationId);link.externalVersion=change.externalVersion||link.externalVersion;link.lastCorrelationId=change.correlationId;link.lastSyncOrigin='CALENDAR';link.lastSyncedFollowUpVersion=updated.version;link.updatedAt=this.clock();this.links.update(link.id,link);return this.record_(change,'APPLIED',updated.id);};
CalendarFollowUpSyncService_.prototype.syncToExternal=function(followUpId,correlationId,connection){var acquired=false,start=this.timer(),providerKey=this.assertTwoWay_().provider;if(this.lock&&typeof this.lock.waitLock==='function'){this.lock.waitLock(30000);acquired=true;}try{var lookupStarted=this.timer(),prior=this.syncEvents.findByCorrelation(providerKey,correlationId),repositoryDuration=this.timer()-lookupStarted;if(prior)return prior;this.measurement_={followUpId:followUpId,connectionId:connection&&connection.id||'',operation:'OUTBOUND_PROJECTION',correlationId:correlationId,totalStartedAt:start,repositoryDurationMs:repositoryDuration};return this.syncToExternalLocked_(followUpId,correlationId,connection);}finally{this.measurement_=null;if(acquired&&typeof this.lock.releaseLock==='function')this.lock.releaseLock();}};
CalendarFollowUpSyncService_.prototype.syncToExternalLocked_=function(followUpId,correlationId,connection){this.assertTwoWay_();var follow=this.followUps.repository.get(followUpId);if(this.followUps.schedulingClassification_(follow)!=='SCHEDULED')throw new VmosValidationError_('A deadline-only FollowUp must be given a Start At, End At, and Time Zone before calendar projection.');var providerKey=this.provider.capabilities().provider,link=this.links.findByFollowUpId(followUpId),sameTarget=link&&link.provider===providerKey&&(!connection||!link.connectionId||link.connectionId===connection.id),payload={followUp:follow,connection:connection||null,externalEventId:sameTarget&&link.externalEventId||'',expectedExternalVersion:sameTarget&&link.externalVersion||'',correlationId:correlationId};try{var providerStarted=this.timer(),result=this.provider.upsert(payload);this.measurement_.providerDurationMs=this.timer()-providerStarted;this.measurement_.mosVersion=follow.version;if(result&&result.conflict){var fields=this.requestFields_(result.currentEvent||{}),request=this.requests.create({id:this.id('ECR'),provider:providerKey,followUpId:followUpId,externalEventId:sameTarget&&link.externalEventId||'',changeType:'CONFLICT',requestedDueAt:fields.requestedDueAt,requestedStartAt:fields.requestedStartAt,requestedEndAt:fields.requestedEndAt,requestedTimeZone:fields.requestedTimeZone,externalVersion:result.externalVersion||'',status:'PENDING_REVIEW',details:'Calendar event version changed before MOS update.',detectedAt:this.clock(),resolvedAt:'',resolvedBy:'',resolution:''});return this.record_({provider:providerKey,correlationId:correlationId,externalEventId:sameTarget&&link.externalEventId||'',changeType:'CONFLICT',externalVersion:result.externalVersion||''},'CONFLICT',request.id);}var saved=link||{id:this.id('FCL'),followUpId:followUpId,connectionId:'',provider:providerKey,calendarId:'',externalEventId:'',externalVersion:'',lastSyncOrigin:'',lastCorrelationId:'',lastSyncedFollowUpVersion:0,createdAt:this.clock()};saved.provider=providerKey;saved.connectionId=connection&&connection.id||'';saved.calendarId=connection&&connection.externalCalendarId||'';saved.externalEventId=result.externalEventId;saved.externalVersion=result.externalVersion||'';saved.lastCorrelationId=correlationId;saved.lastSyncOrigin='MOS';saved.lastSyncedFollowUpVersion=follow.version;saved.updatedAt=this.clock();if(link)this.links.update(saved.id,saved);else this.links.create(saved);return this.record_({provider:saved.provider,correlationId:correlationId,externalEventId:saved.externalEventId,externalVersion:saved.externalVersion},'PUSHED',followUpId);}catch(error){return this.record_({provider:providerKey,correlationId:correlationId,externalEventId:''},'FAILED',error.message);}};
CalendarFollowUpSyncService_.prototype.resolveExternalChange=function(requestId,action,actor,dueAt){var request=this.requests.get(requestId);if(!request||request.status!=='PENDING_REVIEW')throw new VmosValidationError_('External change request is not pending.');var follow=this.followUps.repository.get(request.followUpId),updated=follow;if(action==='RESCHEDULE')updated=this.followUps.reschedule(follow.id,dueAt,follow.version,actor,'review:'+requestId);else if(action==='MARK_COMPLETE')updated=this.followUps.complete(follow.id,follow.version,actor,'review:'+requestId);else if(action==='CANCEL_FOLLOW_UP')updated=this.followUps.cancel(follow.id,follow.version,actor,'review:'+requestId);else if(action!=='KEEP_FOLLOW_UP')throw new VmosValidationError_('Select Keep, Reschedule, Mark Complete, or Cancel Follow-up.');request.status='RESOLVED';request.resolvedAt=this.clock();request.resolvedBy=actor;request.resolution=action;this.requests.update(request.id,request);return updated;};
CalendarFollowUpSyncService_.prototype.record_=function(change,result,details){var measurement=this.measurement_||{},beforePersist=this.timer(),event={id:this.id('CSE'),provider:change.provider||'',followUpId:change.followUpId||measurement.followUpId||'',connectionId:change.connectionId||measurement.connectionId||'',externalEventId:change.externalEventId||'',operation:change.operation||measurement.operation||(change.changeType?'INBOUND_RECONCILE':'OUTBOUND_PROJECTION'),changeType:change.changeType||'',correlationId:change.correlationId||measurement.correlationId||'',mosVersion:change.mosVersion===undefined?(measurement.mosVersion===undefined?'':measurement.mosVersion):change.mosVersion,externalVersion:change.externalVersion||'',result:result,details:details||'',providerDurationMs:Number(change.providerDurationMs||measurement.providerDurationMs||0),repositoryDurationMs:Number(change.repositoryDurationMs||measurement.repositoryDurationMs||0),totalDurationMs:Math.max(0,this.timer()-(change.totalStartedAt||measurement.totalStartedAt||beforePersist)),recoveryRequired:result==='FAILED'||result==='CONFLICT'||result==='REVIEW_REQUIRED',occurredAt:this.clock()};event.repositoryDurationMs+=Math.max(0,this.timer()-beforePersist);return this.syncEvents.append(event);};
